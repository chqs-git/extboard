[package]
name = "extboard-core"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true

# No I/O, no UI, no async. This crate compiles unchanged into extd and into
# the wasm bundle; anything that touches the filesystem or the network belongs
# one level up.
[dependencies]
serde.workspace = true
serde_json.workspace = true
blake3.workspace = true
thiserror.workspace = true
